# payMe-fullstack project

Technologies : Laravel , React, Docker.

# To run the react app


- Clone the project the backend.
- cd to root directory and run.
1) docker-compose build
2) docker-compose up


